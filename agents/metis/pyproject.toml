[project]
name = "kourai-metis"
version = "0.1.0"
description = "Planning agent — transforms ideas into implementation specs"
requires-python = ">=3.12"
dependencies = ["kourai-common"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
