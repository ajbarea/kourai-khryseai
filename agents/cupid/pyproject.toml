[project]
name = "kourai-cupid"
version = "0.1.0"
description = "Cupid — romantic eros, emotional translator, romance coach"
requires-python = ">=3.12,<3.15"
dependencies = ["kourai-common"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
