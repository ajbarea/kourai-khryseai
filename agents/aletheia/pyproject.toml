[project]
name = "kourai-aletheia"
version = "0.1.0"
description = "Aletheia — research validator, citation enforcer"
requires-python = ">=3.12"
dependencies = ["kourai-common"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
