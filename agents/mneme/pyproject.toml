[project]
name = "kourai-mneme"
version = "0.1.0"
description = "Scribe agent — generates commit message groups"
requires-python = ">=3.12"
dependencies = ["kourai-common"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
