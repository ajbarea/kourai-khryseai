[project]
name = "kourai-puck"
version = "0.1.0"
description = "Puck — tutorial daimon, nudge system, gossip broker"
requires-python = ">=3.12"
dependencies = ["kourai-common"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
