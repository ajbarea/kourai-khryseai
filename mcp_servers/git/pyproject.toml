[project]
name = "kourai-mcp-git"
version = "0.1.0"
description = "MCP server for read-only git operations"
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
